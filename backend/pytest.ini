[pytest]
testpaths = tests
python_files = test_*.py
markers =
    smoke: E2E / integration smoke tests
    unit: isolated unit tests
    pressure: backend/database pressure and latency scenarios
addopts = -v -m "not pressure"
